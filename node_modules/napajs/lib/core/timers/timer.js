"use strict";
// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT license.
Object.defineProperty(exports, "__esModule", { value: true });
var TIMEOUT_MAX = Math.pow(2, 31) - 1;
var Timeout = /** @class */ (function () {
    function Timeout(callback, after, repeat, args) {
        if (after < 1)
            after = 0; //0 used for immediate
        if (after > TIMEOUT_MAX)
            after = 1;
        if (repeat < 1 || after == 0)
            repeat = 0; // do not repeat
        if (repeat > TIMEOUT_MAX)
            repeat = 1;
        this._callback = callback;
        this._after = after;
        this._repeat = repeat;
        this._args = args;
        this._timer = null;
        this._active = true;
    }
    return Timeout;
}());
exports.Timeout = Timeout;
