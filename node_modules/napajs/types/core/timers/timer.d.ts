export interface Timer {
}
export declare class Timeout {
    _callback: (...args: any[]) => void;
    private _after;
    private _repeat;
    private _args;
    _active: boolean;
    private _timer;
    constructor(callback: (...args: any[]) => void, after: number, repeat: number, args: any[]);
}
export declare type Immediate = Timeout;
